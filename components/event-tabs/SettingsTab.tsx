'use client';

// This tab is no longer used - event pages have been replaced with static token-based notices.
// Keeping stub for now to avoid breaking imports elsewhere.

export function SettingsTab({ eventId, eventName }: any) {
  return <div>Settings tab disabled</div>;
}
