declare module 'cloudinary' {
  // Minimal ambient declaration to allow importing the Cloudinary SDK in TS builds
  // You can replace this with official types if/when available in the package
  const _default: any;
  export default _default;
  export const v2: any;
}
